private static SparkSession spark() {
    return SparkSession.getActiveSession()
            .orElseGet(() -> SparkSession.getDefaultSession()
                    .orElseThrow(() -> new RuntimeException("No active or default SparkSession found")));
}
