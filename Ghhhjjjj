apiVersion: v1
kind: LimitRange
metadata:
  name: spark-ephemeral-storage
  namespace: 
spec:
  limits:
  - type: Container
    default:
      ephemeral-storage: 100Gi
    defaultRequest:
      ephemeral-storage: 50Gi
